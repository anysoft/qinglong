name: Validate

on:
  pull_request:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: '8.3.1'
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: cp .env.example .env
      - run: sudo apt-get update && sudo apt-get install -y git openssh-client openssl python3 jq perl util-linux ripgrep
      - run: pnpm build:back
      - run: pnpm test
      - run: pnpm build:front
      - run: node diagnostics/phase5/static-audit.cjs
      - name: Typecheck with recorded pre-existing diagnostic budget
        run: node diagnostics/phase5/check-typecheck.cjs
      - name: Install isolated browser acceptance runtime
        run: |
          npm install --prefix "$RUNNER_TEMP/platform-browser" --no-audit --no-fund ssh2@1.17.0 playwright@1.58.2
          node "$RUNNER_TEMP/platform-browser/node_modules/playwright/cli.js" install --with-deps chromium
      - name: Fresh platform browser and restart acceptance
        env:
          QL_BROWSER_RUNTIME: ${{ runner.temp }}/platform-browser/node_modules
        run: pnpm test:platform:e2e
      - run: pnpm benchmark:execution -- --runs=10 --output=execution-baseline.json
      - uses: actions/upload-artifact@v6
        if: always()
        with:
          name: execution-baseline
          path: |
            execution-baseline.json
            diagnostics/phase5/platform-e2e.json
            diagnostics/phase5/platform-e2e-backend.log
            diagnostics/phase5/browser-*.png
            diagnostics/phase5/final-typecheck.*
            diagnostics/phase5/final-static-audit.json
