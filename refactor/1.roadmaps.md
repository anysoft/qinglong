| Phase        | 模块                                             |    当前状态 | 简略目标                                                                                                               |
| ------------ | ---------------------------------------------- | ------: | ------------------------------------------------------------------------------------------------------------------ |
| **Phase 0**  | 现状审计与重构基线                                      |    ✅ 完成 | 梳理现有架构、DB、Subscription、Task、ENV、依赖、执行链，建立 baseline tests 和兼容契约。                                                    |
| **Phase 1**  | Git Credential + Repository                    |    ✅ 完成 | 将 Git 凭据和 Repository 抽成独立资源，支持 PAT/SSH、凭据复用、URL 归一化，同时兼容旧 Subscription。                                            |
| **Phase 2**  | Persistent Git Storage + Worktree + Lock/Lease |    ✅ 完成 | 建立持久 bare repository、`git worktree` 分支工作区、Dirty/Ahead/Behind 状态、FF-only 更新、跨进程锁与 Lease。                            |
| **Phase 3**  | Subscription Git Pipeline v2                   | 🔄 下一阶段 | 把订阅更新从 `rm -rf + clone` 升级为 `Repository fetch + Worktree update + discovery`，但暂时继续 copy 到 `scripts` 并沿用旧 Task 执行链。 |
| **Phase 4**  | Scoped ENV                                     |   ⏳ 待开始 | 增加 `Global ENV → Repo ENV Profile → Task ENV Override`，解决不同项目和任务间环境变量冲突。                                           |
| **Phase 5**  | Config Assets + Hooks v2                       |   ⏳ 待开始 | 将 `.env/config.yaml/json` 等作为独立配置资产，并升级 Hook 为 Before / Success / Failure / Finally。                               |
| **Phase 6**  | Python Runtime Manager                         |   ⏳ 待开始 | 引入 pyenv 管理 Python 多版本，使业务任务 Python 与青龙系统 Python 解耦。                                                               |
| **Phase 7**  | Python venv + Dependency Environment           |   ⏳ 待开始 | 每 Repo/Task 可绑定独立 venv，支持 requirements、pyproject、包安装/升级/删除、pip cache 和可选共享依赖。                                      |
| **Phase 8**  | Node Runtime + Dependency Environment          |   ⏳ 待开始 | 支持 Node 多版本、npm/pnpm/yarn、项目级依赖隔离与共享 pnpm store。                                                                   |
| **Phase 9**  | Task Resource Binding                          |   ⏳ 待开始 | Task 显式绑定 Repository、Worktree、Runtime、venv、ENV、Hooks、cwd、Entrypoint 等资源。                                           |
| **Phase 10** | Unified Execution Resolver / Runner            |   ⏳ 待开始 | 将 Task 解析成统一 ExecutionContext，Runner 只负责 spawn、日志、超时、退出码、信号处理。                                                     |
| **Phase 11** | Task Discovery / Auto Create v2                |   ⏳ 待开始 | 自动发现任务和手工创建任务统一使用同一 Task Model，并继承 Repo 默认 Runtime / ENV / Hooks。                                                  |
| **Phase 12** | Script Editor → Worktree                       |   ⏳ 待开始 | 代码编辑器直接编辑 Worktree，支持 Git Status、Diff、Commit、Push，替代旧 scripts 副本编辑模式。                                              |
| **Phase 13** | Backup / Restore v2                            |   ⏳ 待开始 | 备份 Repository 定义、Task、ENV、Config、Runtime 定义等逻辑资源；Git objects、venv、node_modules 默认可重建。                              |
| **Phase 14** | Legacy Migration & Cleanup                     |   ⏳ 待开始 | 提供旧 Subscription/Task/Dependency 到新模型的迁移，最终逐步淘汰 `/repo` 临时 clone、scripts copy、旧依赖机制等。                              |

